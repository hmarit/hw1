#include <iostream>
#include <string>
#include <set>
#include "func.h"
using namespace std;

int main()
{
  string input, upperInput;

  // Create alphabetically reorganized dictionary
  // ie. butter -> berttu
  alphabeticallyReorderedDict();
  set<string> dictionary;
  cin >>

  /*
  cin >> input;
  upperInput = toUpper(input);
  sortedInput = sortAlphabetically(upperInput);
  */
}
